<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subtotal %>
    <%= form.text_field :subtotal, id: :order_subtotal %>
  </div>

  <div class="field">
    <%= form.label :sales_tax %>
    <%= form.text_field :sales_tax, id: :order_sales_tax %>
  </div>

  <div class="field">
    <%= form.label :grand_total %>
    <%= form.text_field :grand_total, id: :order_grand_total %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :order_user_id %>
  </div>

  <div class="field">
    <%= form.label :order_items %>
    <%= form.text_area :order_items, id: :order_order_items %>
  </div>

  <div class="field">
    <%= form.label :handling_fee %>
    <%= form.text_field :handling_fee, id: :order_handling_fee %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
